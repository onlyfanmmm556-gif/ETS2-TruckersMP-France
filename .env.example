# Token du bot Discord (Developer Portal > Bot > Token)
DISCORD_TOKEN=your_discord_bot_token_here

# ID du salon Discord où envoyer les stats
CHANNEL_ID=your_channel_id_here

# Intervalle de mise à jour (format cron)
# "*/30 * * * *"  = toutes les 30 minutes (défaut)
# "0 * * * *"     = toutes les heures
# "0 */2 * * *"   = toutes les 2 heures
# "0 8,20 * * *"  = 2x par jour (8h et 20h)
STATS_INTERVAL=*/30 * * * *

# Nombre de VTCs dans le classement (défaut: 18)
MAX_COMPANIES=18

# Nom du bot — utilisé comme User-Agent pour l'API Trucky (ne pas laisser vide)
BOT_NAME=TruckyStatsBotFR

# Jeu : 1 = ETS2 (défaut), 2 = ATS
GAME=1

# Type de stats : real_miles (défaut) | driven_miles
STATS_TYPE=real_miles
